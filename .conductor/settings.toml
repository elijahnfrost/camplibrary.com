"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run = "npm run dev -- --hostname 127.0.0.1 --port ${CONDUCTOR_PORT:-3000}"
run_mode = "concurrent"
setup = "npm install"
